import React from 'react'

const SocialMedia = ({icons}) => {
    return (
        <>
            <div  className="social-profile me-4  text-center">
                <i className={`${icons}`}></i>
            </div>
        </>
    )
}

export default SocialMedia
